'use client';

import AppLayout from '@/components/AppLayout';
import VideoGenerationClient from './components/VideoGenerationClient';

export default function VideoGenerationPage() {
  return (
    <AppLayout>
      <VideoGenerationClient />
    </AppLayout>
  );
}
